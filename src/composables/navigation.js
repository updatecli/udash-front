import { ref } from 'vue'

// Whether the navigation drawer is open on narrow screens, where it is a temporary
// drawer opened from the app bar instead of the permanent rail.
export const navDrawerOpen = ref(false)
